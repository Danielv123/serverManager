{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@ant-design/colors": "^4.0.5",
    "@ant-design/icons": "^4.7.0",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^11.2.2",
    "antd": "^4.20.7",
    "react": "^17.0.1",
    "react-contexify": "^4.1.1",
    "react-dom": "^17.0.1",
    "react-scripts": "5.0.0",
    "socket.io-client": "^2.4.0",
    "socket.io": "^2.4.1",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "npx serve -l 8080 -s ./build",
    "dev": "PORT=3001 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "format": "prettier --loglevel warn --write \"src/**/*.{jsx,html,src,js,css,md}\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
